/*TODO: per ora dummy handlers */
void sysbp(){ 
	debug(1, 155);
	while(1);
}

void trap(){
	debug(2, 155);
	while(1);
}

void tlb(){
	debug(3, 155);
	while(1);
}

void ints(){
	debug(4, 155);
	while(1);
}
